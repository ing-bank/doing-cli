
from doing.utils import run_command

def _status(team: str, area: str, iteration: str, organization: str, project: str):

    # Show:
    # - src path of config file
    # - current git branch
    # - if there is a ref on origin with the same branch?
        # - if that ref has any linked work items
        # - if that ref has any linked pull requests
    
 
    # Branch: 99035_windows_bug
    # Linked work item ID: xxx
    # Pull request ID: xxx
    # 
    pass
